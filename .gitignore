.idea
node_modules/
dist/
.playwright-mcp/

# Temporary discovery captures (kept locally, not committed)
*.png
*-snapshot.yml

# App icons — shipped with the extension
!public/icons/*.png

# README screenshots — committed so GitHub renders them
!screenshot/*.png

# Tooling
.superpowers/
.omc/
.claude/
